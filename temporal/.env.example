# Internal Postgres credentials used by the Temporal cluster (not exposed externally)
POSTGRES_USER=temporal
POSTGRES_PASSWORD=temporal
POSTGRES_DB=temporal

# Temporal UI port on the host (mapped from container 8080)
TEMPORAL_UI_PORT=8089
